{
  "name": "loom",
  "version": "1.0.0",
  "repository": "https://github.com/kyle1320/loom",
  "author": "Kyle Cutler <kyle1320@gmail.com>",
  "license": "GPL-3.0-or-later",
  "private": true,
  "devDependencies": {
    "@types/jest": "^24.9.1",
    "@typescript-eslint/eslint-plugin": "^2.17.0",
    "@typescript-eslint/parser": "^2.17.0",
    "eslint": "^6.8.0",
    "jest": "^25.1.0",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^25.0.0",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "lint": "eslint packages --ext js,ts,jsx,tsx",
    "typecheck": "npm-run-all -p core-typecheck data-typecheck ui-typecheck",
    "verify": "yarn run typecheck && yarn run lint",
    "test": "jest",

    "core-typecheck": "yarn workspace loom-core run typecheck",
    "build-core": "yarn workspace loom-core run build",

    "data-typecheck": "yarn workspace loom-data run typecheck",
    "build-data": "yarn workspace loom-data run build",

    "ui": "yarn workspace loom-ui run dev",
    "ui-dist": "yarn workspace loom-ui run dist",
    "ui-typecheck": "yarn workspace loom-ui run typecheck"
  },
  "workspaces": [
    "packages/*"
  ]
}
